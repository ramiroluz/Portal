default_context:
    initial_user_name: 'admin'
    initial_user_password: 'admin'
    zcml_package_includes: ['camara_de_curitiba', 'pas.plugins.oidc', 'pas.plugins.keycloakgroups']

    db_storage: direct
